interface:
  display_name: "Oh My Frontend"
  short_description: "Lightweight frontend triage and orchestration"
  default_prompt: "Use $oh-my-frontend to classify frontend work; add planning, approval, workers, and durable docs only when the changed risk requires them."

policy:
  allow_implicit_invocation: true
